import React from 'react';
import Title from '../../shared/Title/Title';

const NotFound = () => {
    return (
        <div>
            <Title
             title={NotFound}></Title>
            THis is a not founds 
        </div>
    );
};

export default NotFound;